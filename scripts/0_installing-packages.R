### Installing all the packages and loading GSS data ###

# This process may take 15-20 minutes.

# Wait until the "Stop" sign in the console disappears and 
#    no more code is running in the console.

# After all the packages are installed (no "Stop" sign, no codes running in the console),
# You should see "gss" and "key" under the "Environment - Data" part (top right)

# If you see "gss" and "key" under the "Environment - Data" part (top right),
# everything is all set.

